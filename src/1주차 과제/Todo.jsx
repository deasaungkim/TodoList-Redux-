


function Todo() {



  return (
    <div>
      {/* <h1>Working</h1>

      <div>
        {todos.map((items) => {
          if (!items.isDone) {
            return (
              <div>
                <div>{items.title}{items.content}</div>
                <div>
                  <button onClick={() => deleteHandler(items.id)}>삭제하기</button>

                  <button onClick={() => editHandler(items.id)}>{items.isDone ? "취소" : "완료"}</button>
                </div>
              </div>)
          }
        })}
      </div>


      <h1>Done</h1>


      <div>
        {todos.map((items) => {
          if (items.isDone) {
            return <div><div>{items.title}{items.content}</div>
              <div>
                <button onClick={() => deleteHandler(items.id)}>삭제하기</button>

                <button onClick={() => editHandler(items.id)}>{items.isDone ? "취소" : "완료"}</button></div></div>
          }
        })}
      </div> */}
    </div>


  )
}
export default Todo;